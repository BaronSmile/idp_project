Рекомендуется установить зависимости для client и server войти в каждую папку:
### `npm install`
Для запуска перейти в папку 'ИПР_2':
### `npm run start`
команда запустит серверную и клиентскую часть локально.